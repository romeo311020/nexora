# تحويل EDUVO لتطبيق (PWA) — خطوات الرفع

## 1) بنية الملفات المطلوبة على GitHub

لازم تترفع بنفس البنية دي، الأيقونات جوه فولدر اسمه `icons`:

```
/ (جذر الريبو)
├── index.html
├── admin.html
├── data.json
├── manifest.json
├── service-worker.js
└── icons/
    ├── icon-192.png
    ├── icon-512.png
    └── icon-maskable-512.png
```

⚠️ **مهم:** فولدر `icons` والملفين `manifest.json` و `service-worker.js` لازم يكونوا في **نفس المجلد** اللي فيه `index.html` بالظبط.

## 2) الرفع على GitHub

```
git add index.html manifest.json service-worker.js icons/
git commit -m "تحويل EDUVO إلى تطبيق PWA قابل للتثبيت"
git push
```

## 3) تجربة التثبيت

بعد ما GitHub Pages ينشر (دقيقة أو اتنين):

### على أندرويد (Chrome):
1. افتح رابط الموقع
2. هيظهر تلقائيًا إشعار "إضافة EDUVO إلى الشاشة الرئيسية" في الأسفل، أو من قائمة ⋮ اختر **"تثبيت التطبيق"**
3. هيتحمل زي أي تطبيق عادي بأيقونته الخاصة، وهيفتح في نافذة منفصلة بدون شريط المتصفح

### على آيفون (Safari):
1. افتح رابط الموقع
2. دوس على زر المشاركة (المربع بالسهم) ⬆️
3. اختار **"Add to Home Screen" / "إضافة إلى الشاشة الرئيسية"**

## ما الذي يعمل فعليًا؟

- ✅ **التثبيت الحقيقي:** التطبيق هيتحمل بأيقونة، بدون شريط عنوان المتصفح، زي أي تطبيق native
- ✅ **يعمل بدون إنترنت جزئيًا:** الصفحات اللي الطالب زارها قبل كده (الواجهة، الأنماط، الخطوط) هتفتح حتى لو النت وقع
- ⚠️ **محتاج إنترنت فعلي لـ:** تسجيل الدخول بجوجل، تحميل بيانات `data.json` المحدثة، المساعد الذكي (لو مفعّل API)، الكورسات المجانية (الفيديوهات نفسها)

هذا PWA حقيقي (Progressive Web App) — مش تطبيق من متجر Google Play أو App Store، لكنه بيتصرف زي تطبيق مثبت 100% على الجهاز.
