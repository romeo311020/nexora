# هيكلة EDUVO الجديدة — ملفات منفصلة

تم تقسيم `index.html` (اللي كان ملف واحد ضخم) إلى ملفات منظمة كل قسم في مكانه، مع الحفاظ على نفس الشكل والوظائف 100% (اتفحص بالكامل بمتصفح حقيقي، صفر أخطاء).

## البنية الجديدة

```
/
├── index.html              ← الهيكل العام بس (Sidebar, Topbar, Modals) + تحميل باقي الأقسام
├── css/
│   └── styles.css          ← كل التنسيقات (كانت جوه <style> في نفس الملف)
├── js/
│   └── app.js               ← كل منطق التطبيق (كان جوه <script> في نفس الملف)
├── screens/                 ← كل شاشة في ملف منفصل، بيتحمّلوا تلقائيًا عند فتح الموقع
│   ├── auth.html             (تسجيل الدخول / إنشاء حساب)
│   ├── dashboard.html        (الرئيسية)
│   ├── subjects.html         (المواد الدراسية)
│   ├── subject-detail.html   (تفاصيل مادة + المدرسين)
│   ├── ai.html                (المساعد الذكي)
│   ├── books.html             (الكتب)
│   ├── planner.html           (خطتي)
│   ├── courses.html           (كورسات مجانية)
│   ├── lecture.html           (مشاهدة محاضرة)
│   └── profile.html           (حسابي)
├── icons/                   ← أيقونات PWA (زي ما هي)
├── data.json                ← البيانات (زي ما هي، مفيش أي تغيير)
├── manifest.json            ← إعدادات PWA (زي ما هي)
├── service-worker.js        ← محدّث ليشمل الملفات الجديدة في التخزين المؤقت
└── admin.html                ← لوحة التحكم (زي ما هي، منفصلة أصلاً)
```

## إزاي شغال ده تقنيًا؟

`index.html` بقى فيه حاويات فاضية لكل شاشة:
```html
<section id="screen-auth" class="screen active"></section>
<section id="screen-dashboard" class="screen"></section>
...
```

وأول ما الموقع يفتح، `js/app.js` بيجيب محتوى كل شاشة من `screens/` ويحطه جوه الحاوية بتاعتها، وبعدين يشغّل باقي التطبيق زي ما هو بالظبط.

## ⚠️ مهم جدًا عند الرفع على GitHub

لازم **ترفع كل الفولدرات والملفات دي مع بعض بنفس البنية بالظبط** — من غير ما تغيّر أسماء الفولدرات (`css`, `js`, `screens`) لأن `index.html` بيدور عليهم بالاسم ده بالظبط.

```
git add index.html css/ js/ screens/ service-worker.js
git commit -m "تقسيم الملف الواحد إلى ملفات منظمة"
git push
```

## الفايدة من التقسيم

- **سهولة التعديل:** عايز تعدّل شاشة "خطتي" بس؟ افتح `screens/planner.html` مباشرة من غير ما تدور وسط 3000 سطر
- **سهولة التنسيق:** كل التصميم في `css/styles.css` منفصل تمامًا عن المنطق والمحتوى
- **قابلية التوسع:** أي شاشة جديدة مستقبلًا = ملف جديد في `screens/` بس

## ملحوظة عن الأداء

تحميل الشاشات بيحصل بطلبات `fetch` صغيرة ومتوازية (كلهم بيتحملوا في نفس الوقت مش واحد ورا التاني)، فمفيش أي فرق ملحوظ في سرعة فتح الموقع مقارنة بالملف الواحد.
